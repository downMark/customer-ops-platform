{
  "name": "customer-ops-performance-sentry-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "sync": "node --env-file-if-exists=.env sync.mjs",
    "tail": "node --env-file-if-exists=.env tail-to-sentry.mjs",
    "test": "node --test *.test.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.850.0"
  }
}
